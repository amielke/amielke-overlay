--- a/variety_lib/varietyconfig.py
+++ b/variety_lib/varietyconfig.py
@@ -49,6 +49,14 @@ def get_data_path():
     # Look for data files in the source tree
     possible_paths = [
         os.path.join(os.path.dirname(__file__), "..", "data"),
+        # Gentoo / standard Linux system paths
+        "/usr/share/variety/data",
+        "/usr/share/variety",
+        "/usr/local/share/variety/data",
+        "/usr/local/share/variety",
+        # Fallback for distributions that install under /usr/share
+        "/usr/share",
+        "/usr/local/share",
     ]

     for path in possible_paths:

